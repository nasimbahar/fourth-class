mod example;
mod stdadding;
mod derive;
mod operatorover;
mod sized;
mod defaults;
mod clonecopy;
mod Conversion;
mod refconversion;
mod drop;
mod lifetime;
mod Traitexample;
fn main() {
    //Traitexample::starting_point();
   //example::starting_point();
     
   // stdadding::starting_point();
   // derive::starting_point();
   // operatorover::starting_point();
   // sized::starting_point();
   //defaults::starting_point();
    //clonecopy::starting_point();
  // Conversion::starting_point();
   //refconversion::starting_point();
   // drop::starting_point();
   lifetime::starting_point();

    
}
